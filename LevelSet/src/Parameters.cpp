//-----------------------------------------------------------------------------------------------------------------------------
#include "Parameters.h"
//-----------------------------------------------------------------------------------------------------------------------------
using namespace kmd;

//=============================================================================================================================
Parameters::Parameters()
//=============================================================================================================================
        : time_step_(0.0),
          time_step_number_(0),
          space_step_(0.0),
          image_width_(0),
          image_height_(0),
          constant_speed_(1.0),
          epsilon_(0.0),
          sigma_(0.0)
{
}

#ifdef UNIT_TEST_Parameters
#define BOOST_TEST_DYN_LINK
//-----------------------------------------------------------------------------------------------------------------------------
#include <kmd/without_warning/unit_test.h>
#include <iostream>
//-----------------------------------------------------------------------------------------------------------------------------
using namespace std;

//=============================================================================================================================
BOOST_AUTO_TEST_CASE(TEST_Parameters) {
//=============================================================================================================================

        cout << "Parameters\n";

        {
                Parameters params;
                BOOST_CHECK(params.sigma_ == 0.0);
                BOOST_CHECK(params.time_step_number_ == 0);
                BOOST_CHECK(params.space_step_ == 0.0);
                BOOST_CHECK(params.image_width_ == 0);
                BOOST_CHECK(params.image_height_ == 0);
                BOOST_CHECK(params.constant_speed_ == 1.0);
                BOOST_CHECK(params.epsilon_ == 0.0);

        }

}
#endif // UNIT_TEST_Parameters


